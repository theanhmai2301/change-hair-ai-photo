<?xml version="1.0" encoding="utf-8"?>
<!-- Secondary CTA: soft tinted surface + emerald border (theme-aware). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/btn2_bg" />
    <stroke android:width="1.5dp" android:color="@color/btn2_border" />
    <corners android:radius="24dp" />
</shape>
